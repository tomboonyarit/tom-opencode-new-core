# Go API Server

Simple REST API built with Gin framework.

## Setup

```bash
cd src/api
go mod tidy
```

## Run

```bash
go run main.go
```

The API will be available at `http://localhost:8080`

## Endpoints

- `GET /hello` - Returns a Hello World message